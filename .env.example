# This is an example file. Copy it to .env.local and fill in your actual values.
# .env.local is ignored by Git and should contain your secret credentials.

# --- Database (PostgreSQL with SSL) ---
# Your full database connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=verify-full"
# The local path to your database's SSL certificate
DATABASE_CERT_PATH="root.crt"

# --- Email (Nodemailer with Gmail) ---
# The SMTP host for your email provider
EMAIL_HOST="smtp.gmail.com"
# The SMTP port for your email provider
EMAIL_PORT=587
# Your Gmail address
EMAIL_USER="your-email@gmail.com"
# A 16-character Google App Password. See https://support.google.com/accounts/answer/185833
EMAIL_PASS="your16digitapppassword"

# --- Firebase for Social Login ---
# Client-side config. Found in your Firebase project settings (Web app). Must be enclosed in single quotes.
NEXT_PUBLIC_FIREBASE_CONFIG='{"apiKey":"AIza...","authDomain":"...","projectId":"...",...}'
# Server-side config. Generated from your project's "Service accounts" tab. Must be enclosed in single quotes.
FIREBASE_ADMIN_CONFIG='{"type":"service_account","project_id":"...",...}'
